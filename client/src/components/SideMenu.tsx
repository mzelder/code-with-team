function SideMenu() {
    return (
        <h1 className="text-white">Dashboard</h1>
    );
}

export default SideMenu;

//TODO 
// Authorize each root, if the root is for logged users then the api should make a call into api and check
// if its correct